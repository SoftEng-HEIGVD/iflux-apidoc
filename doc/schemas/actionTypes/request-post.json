{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Action type",
  "type": "object",
  "properties": {
    "name": {
      "description": "A name to identify the action type.",
      "type": "string"
    },
    "description": {
      "description": "A human description of the action type.",
      "type": "string"
    },
    "type": {
      "description": "Valid URL to define the action type.",
      "type": "url"
    },
    "actionTargetTemplateId": {
      "description": "The action target template id to link with the action type",
      "type": "integer"
    },
    "schema": {
      "description": "A valid JSON Schema.",
      "type": "json"
    }
  },
  "required": [ "name", "type", "actionTargetTemplateId", "schema" ]
}