{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Event types",
  "description": "The collection of event types",
  "type": "array",
  "items": {
    "title": "Event types.",
    "type": "object",
    "properties": {
      "id": {
        "description": "Unique identifier given by the server",
        "type": "integer"
      },
      "key": {
        "description": "Unique key generated by the server to be used in events.",
        "type": "string"
      },
      "name": {
        "description": "A name to identify the event type.",
        "type": "string"
      },
      "description": {
        "description": "A human description of the event type.",
        "type": "string"
      },
      "eventSourceTemplateId": {
        "description": "The event source template id to link with the event type",
        "type": "integer"
      },
      "schema": {
        "description": "A valid JSON Schema.",
        "type": "json"
      }
    },
    "required": [ "id", "key", "name", "eventSourceTemplateId", "schema" ]
  }
}